# 复习计划系统测试清单

## 本轮新增重点
1. 复习计划页是否新增独立的 `知识点层级` 统计卡。
2. `知识点层级` 里是否能看到：
   - 核心
   - 重点
   - 常规
   - 错题
   四类数量。
3. 复习计划页是否新增独立的 `错题复盘` 区块，而不是只在单条复习项里看到错题标记。
4. `错题复盘` 区块里列出的是否确实是已标记为错题型的知识点。
5. 调整某条复习项的错题标记后，`错题复盘` 和 `知识点层级` 的数量是否会同步变化。
6. 调整某条复习项的重要度后，`知识点层级` 的核心/重点/常规数量是否会同步变化。

1. 课程笔记编辑区是否出现 `知识点标签建议`，并会根据标题、正文自动给出建议标签。
2. 课程笔记编辑区中，当前标签是否会单独显示为可点击的标签胶囊，点击后可直接移除。
3. 点击建议标签后，标签输入框内容是否会立即同步增删。
4. 课程笔记卡片里是否新增 `知识点：...` 预览摘要。
5. 复习计划页的 `考试周复习冲刺` 是否新增第二行模板切换：
   - `均衡推进`
   - `考试冲刺`
   - `错题回炉`
   - `快速回忆`
6. 切换不同冲刺模板后，冲刺项顺序、数量或建议投入是否会发生变化。
7. `课程记忆画像` 卡片里是否新增：
   - 质量分数
   - 重点项数量
   - 核心项数量
   - 错题项数量
8. 课程分组头部是否也能看到课程级质量摘要，而不只是待处理数量和平均记忆状态。
9. 复习项卡片中的 `知识点质量` 操作后，课程画像中的质量统计是否跟着变化。
10. `错题回炉` 模板下，带错题标记的复习项是否明显前置。
11. `快速回忆` 模板下，短时长复习项是否明显前置。
12. `考试冲刺` 模板下，临近考试相关知识点是否明显前置。

1. `考试周复习冲刺` 卡里是否出现类型筛选：`全部 / 只看考试 / 只看作业 / 只看复习`。
2. 切换冲刺类型筛选后，冲刺列表、统计卡、提交到智能入口的内容是否一起变化。
3. 冲刺区是否出现两张统计卡：
   - `冲刺规模`
   - `建议投入`
4. `建议投入` 是否会反映当前筛选下的总分钟数与临近项数量。
5. 点击 `生成今日冲刺` 后，待办里是否批量生成带 `考试周冲刺` 和 `今日冲刺` 标签的复习任务。
6. `生成今日冲刺` 后，新任务备注里是否带有来源笔记和“今日冲刺建议”说明。
7. 点击 `转考试周待办` 后，是否只导出当前筛选下的冲刺项，而不是未筛选的全部项。
8. 点击 `提交冲刺上下文` 后，首页智能体里的 `考试周复习冲刺` 摘要是否反映：
   - 冲刺项数量
   - 临近项数量
   - 建议投入分钟数
9. 复习项卡片里是否新增 `知识点质量` 区块。
10. `知识点质量` 里是否能看到：
   - 重要度
   - 校准后掌握度
   - 错题型状态
11. 点击 `切换重要度` 后，复习项的重要度是否按 `常规 -> 重点 -> 核心 -> 常规` 循环。
12. 点击 `标记错题 / 取消错题` 后，复习项状态是否立即变化并持久化。
13. 点击 `上调掌握度 / 下调掌握度` 后，复习项的校准掌握度显示是否立即变化。
14. 手动校准掌握度后，智能重排顺序是否会受到影响。
15. 把高重要度或错题型复习项加入待办后，待办里的：
   - 优先级
   - 标签
   - 备注中的重要度/掌握度说明
   是否都能跟着变化。

## 核心复习流程
1. `智能重排` 是否会改变当前建议顺序。
2. `智能重排建议` 文案是否能体现遗忘风险、考试周临近度和建议时长。
3. 每门课的 `整组完成 / 整组延后` 是否生效。
4. `按空档生成当天安排` 后，待办里是否出现带时间段说明的复习任务。
5. `完成率趋势 / 课程记忆画像 / 今日批量计划` 三块是否正常显示。
6. 课程分组的 `展开明细 / 收起明细` 是否稳定。
7. 复习项如果命中考试周事件，是否能看到相关提示。
8. 完成一条复习项后，状态文案里是否带出下一轮建议。
9. `学习数据` 页中的复习指标、结构和风险提醒是否会跟随复习状态变化。
10. 点击 `应用建议到待办` 后，智能重排前几项是否能批量写入待办。

## 智能接管与执行
1. 点击 `提交到智能入口` 后，首页智能体的 `可用上下文` 中是否出现 `待接管复习计划`。
2. 点击 `待接管复习计划` 摘要卡后，当前会话里是否出现 `已注入复习计划` 的系统消息。
3. 注入后输入框是否会自动带出接管型 prompt。
4. 在智能体里输入“接管复习计划 / 安排今天复习 / 执行复习 / 开始复习计划 / 把复习计划落地”后，是否会触发 `执行复习计划`。
5. 执行后，待办里是否新增 `assistant-review-...` 前缀的复习任务。
6. 执行后，番茄钟是否自动绑定到首条复习任务。
7. 智能体模块里是否出现 `复习计划执行结果` 卡。
8. 点击 `查看待办 / 查看番茄钟` 是否能跳到正确模块。
9. 点击 `撤销执行` 后，是否会移除这次智能体生成的复习待办。
10. 点击 `撤销执行` 后，如果当前番茄绑定正是该复习任务，是否会一起清除。

## 历史、回放与差异
1. 智能体模块里是否出现 `最近接管历史`。
2. 多次执行复习计划后，历史记录是否会累积更新，而不是只覆盖最后一次。
3. `最近接管历史` 是否支持 `展开明细 / 收起明细`。
4. 展开历史后，是否能看到当次生成的复习待办标题列表。
5. 展开历史后，是否能看到当次绑定到番茄钟的复习任务标题。
6. 点击某条接管历史的 `回放方案` 后，是否会重新生成一组复习待办。
7. `回放方案` 后，番茄钟是否会恢复绑定到该次历史里的复习任务。
8. `回放方案` 后，首页的最新执行结果和最近接管历史是否都会同步刷新。
9. 对于通过 `回放方案` 生成的新结果，明细里是否会出现“回放自某次执行”的来源提示。
10. 回放后的明细里是否会显示“与原方案相比”的差异摘要。
11. 差异摘要里的任务数量和番茄绑定描述是否与实际回放结果一致。

## 历史与复习页双向定位
1. 点击 `回到复习页` 后，是否会直接打开复习计划页。
2. 从结果卡或历史卡进入复习页后，是否会自动切到 `全部知识点` 并带入对应搜索词。
3. 从结果卡或历史卡进入复习页后，是否会自动展开命中的课程分组。
4. 命中的复习项卡片是否会出现明显高亮。
5. 复习项卡片里如果存在智能接管来源，是否会显示 `智能接管来源` 提示卡。
6. 点击复习项上的 `查看接管历史` 后，是否会返回首页并展开对应那次接管历史。
7. 从复习页返回首页后，目标历史记录是否已经自动展开，而不需要手动再点一次。

## 结构化关联
1. 新执行一次复习计划后，对应复习项是否写入稳定关联，而不是只靠标题匹配。
2. 回放历史后，对应复习项是否更新为新的接管记录来源。
3. 撤销最近一次执行后，对应复习项上的 `智能接管来源` 是否会一起清除。
4. 旧历史记录在没有结构化关联 id 的情况下，是否仍能通过兼容回退正常显示来源提示。

## 考试周冲刺联动
1. 复习计划页是否存在独立的 `考试周复习冲刺` 区块，而不是只在普通排序里体现。
2. 冲刺区是否能列出与考试 / 作业 / 复习事件直接相关的知识点。
3. 点击 `转考试周待办` 后，是否会批量生成带 `考试周冲刺` 标签的高优先复习待办。
4. 点击 `绑定冲刺专注` 后，番茄钟是否会带入最紧急的一条冲刺项。
5. 点击 `提交冲刺上下文` 后，首页智能体 `可用上下文` 里是否出现 `考试周复习冲刺`。
6. 点击 `考试周复习冲刺` 摘要卡后，输入框是否会自动带出偏向冲刺安排的 prompt。

## 联动检查
1. 课程笔记新增或修改后，复习项是否会自动同步。
2. 一键加入待办后，任务是否带有：
   - 复习标签
   - 子任务
   - 番茄目标轮次
3. 一键绑定专注后，番茄钟是否带入对应复习项。
4. 首页 `学习推进` 中的复习摘要卡是否仍正常跳转。
5. 完成复习项后，`下一轮建议` 卡是否会更新。
6. 首页智能体里与复习相关的提问，是否会优先命中复习计划摘要。
7. 智能体执行复习计划后，待办、番茄钟和复习桥接状态是否同步更新。
8. 撤销执行后，智能体会话里是否会追加一条系统反馈消息。
9. 复习页手动推进知识点后，智能桥接进度是否和复习页当前状态保持一致。
10. 智能体执行复习计划后，首页结果卡与历史列表是否同步刷新。
11. 复习计划提交到智能入口后，如果其中包含考试周相关知识点，智能体上下文里是否同时保留普通复习计划和冲刺摘要。

## 通过标准
- 不出现点击无反应。
- 不出现编译报错。
- 复习排序、考试周提示、待办联动、番茄联动、智能体桥接、历史明细、历史回放、回放差异对比、回到复习页定位、从复习页反向查看历史、结构化关联 id，以及考试周复习冲刺上下文都能真实生效。

## 本轮新增重点
1. `错题复盘` 区块里的每条错题知识点是否新增 `回到原笔记`。
2. 点击 `回到原笔记` 后，是否会直接打开课程笔记页，并自动定位到对应课程与来源笔记标题。
3. `课程维度复盘` 是否作为独立区块出现，而不是继续混在 `课程记忆画像` 里。
4. `课程维度复盘` 中每门课是否能看到：
   - 待处理数量
   - 错题数量
   - 核心项数量
   - 建议投入分钟数
5. `课程维度复盘` 中是否会明确给出“本课当前最该补强”的知识点。
6. 点击课程维度复盘里的 `回到原笔记` 后，是否也能直接跳回对应课程笔记来源。
7. 从复习页跳到课程笔记后，原有的课程筛选和笔记搜索是否仍然正常工作。

## 本轮新增重点
1. 课程笔记编辑区是否新增 `知识点结构化编辑` 输入区，而不是只靠正文自动拆分。
2. 在 `知识点结构化编辑` 中手动录入的知识点，保存后是否会真正覆盖复习计划的知识点来源。
3. 课程笔记编辑区是否出现：
   - `当前知识点`
   - `知识点建议`
   两组辅助操作。
4. 点击 `知识点建议` 后，是否会把建议知识点真正写入结构化知识点列表。
5. 点击 `当前知识点` 胶囊上的移除操作后，是否会从结构化知识点列表里即时删掉。
6. 课程笔记卡片里的 `知识点：...` 预览，是否会优先显示结构化知识点，而不是只从正文截取。
7. 复习计划页是否新增 `复习项批量标签` 区块。
8. 输入批量标签后，点击 `应用到当前筛选`，当前筛选下的复习项标签是否会统一追加。
9. 输入批量标签后，点击 `应用到今日计划`，今日应复习的复习项标签是否会统一追加。
10. 输入批量标签后，点击 `从当前筛选移除`，当前筛选下的同名标签是否会统一移除。
11. 批量标签操作后，复习项卡片上的标签展示、搜索筛选和转待办标签是否都还能保持一致。

## 本轮新增重点
1. `错题来源统计` 是否作为独立区块出现，而不是只在错题复盘里逐条看来源。
2. `错题来源统计` 是否会按“课程 + 来源笔记”聚合显示高频错题来源。
3. `复习标签画像` 是否作为独立区块出现，并显示当前复习项中最常见的标签分布。
4. 复习项执行批量标签操作后，`复习标签画像` 的标签计数是否会同步变化。
5. 考试周冲刺模板里是否新增 `自定义模板`。
6. 选中 `自定义模板` 后，是否会出现：
   - 焦点选择（如错题优先 / 核心优先 / 短时优先 / 临近优先）
   - 保留项数选择
7. 切换不同自定义焦点后，冲刺项排序是否会真实变化，而不是只改文案。
8. 修改 `保留项数` 后，冲刺列表数量是否会真实变化。
9. `模板偏向` 卡在自定义模板下，是否会显示当前焦点和保留项数，而不是沿用默认模板说明。

## 本轮新增重点
1. 复习计划页顶部操作区是否新增 `导出复习` 按钮。
2. 点击 `导出复习` 后，是否会直接进入导出中心。
3. 导出中心是否新增 `复习计划摘要` 导出项。
4. `复习计划摘要` 是否会包含：
   - 命中复习项数量
   - 逾期数量
   - 错题数量
   - 核心项数量
   - 高频来源笔记
5. 切换导出范围或导出对象后，`复习计划摘要` 预览是否会真实变化。
6. 复习计划页是否新增独立的 `错题专题复盘` 区块。
7. `错题专题复盘` 是否会优先列出高风险或临近的错题型知识点，而不是随机列出。
8. `错题专题复盘` 的内容是否和 `错题复盘 / 错题来源统计` 相互一致，不出现数量冲突。

## 本轮新增重点
1. 复习计划页是否新增独立的 `复习日历视图` 区块，并按未来两周展示每日复习负载。
2. 点击复习日历中的某一天后，下面复习列表是否会切到该日期，而不是只停留在原筛选口径。
3. 再次点击已选中的日期后，日期筛选是否会被清除并恢复完整列表。
4. 复习日历卡片里是否能看到当天的 `应复习 / 已完成 / 逾期` 摘要，而不是只有日期。
5. 复习计划页是否新增独立的 `错题回炉专注模式` 区块。
6. `错题回炉专注模式` 是否会显示：
   - 回炉规模
   - 建议专注分钟数
   - 首要回炉项
7. 点击 `开启回炉专注` 后，番茄钟是否会绑定到一条以 `错题回炉：...` 命名的专项任务。
8. 点击 `生成回炉计划` 后，待办里是否会批量生成带 `错题回炉 / 复习强化` 标签的专项复习任务。
9. `生成回炉计划` 后，这些任务是否进入独立的 `错题回炉` 清单，而不是继续混在普通复习清单里。
10. 错题回炉专注模式里的首要回炉项，是否会随着错题标记、重要度、考试周临近度变化而变化。

## 2026-04-01 本轮新增测试
### 课程笔记附件
1. 课程笔记编辑区是否新增 `插入图片 / PPT / PDF` 按钮。
2. 点击插入后，是否可以正常选择图片、PDF、PPT 文件。
3. 同一次编辑里连续插入多个附件后，编辑区是否会列出附件名称、类型和大小。
4. 编辑区里的附件点击 `打开` 后，是否能正常读取对应图片、PDF、PPT。
5. 编辑区里的附件点击 `移除` 后，是否会立刻从当前草稿中移除。
6. 保存新笔记后，笔记卡片里是否会显示 `附件 X 个` 以及附件名称标签。
7. 在笔记卡片里点击附件标签后，是否仍能正常打开对应文件。
8. 编辑已有笔记并新增附件后，保存修改，旧附件和新附件是否都会保留。
9. 编辑已有笔记并移除某个旧附件后，保存修改，该附件是否会真正消失且无法再次打开。
10. 删除整条笔记后，这条笔记关联的附件是否会一并删除，不会残留旧文件。

### 白噪音 / 环境声
1. 番茄钟页是否出现环境声预设切换项。
2. `山风` 是否能正常播放。
3. `溪流` 是否能正常播放。
4. `雨声` 是否能正常播放。
5. `海浪` 是否能正常播放。
6. `篝火` 是否能正常播放。
7. `白噪音` 是否能正常播放。
8. `粉噪` 是否能正常播放。
9. 如果当前设备无法启动环境声，是否会提示失败并自动关闭白噪音开关。
